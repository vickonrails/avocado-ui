---
title: Input
section: Components
---

import { LiveConfig } from 'smooth-doc/components'

<!-- Define `@avocado-ui/react` module as `{ Input }` -->

<LiveConfig modules={{ '@avocado-ui/react': { Input } }} />

# Input

Input Component

<Input
  type='email'
  borderRadius='round'
  placeholder="Something You don't want to joke with"
/>
